import axios from "axios";

const UserInfo = (props) => {
  //   const motoData = axios.get(`http://localhost:5000/user/${props.userId}`);
  //   console.log(motoData);
  return <>UserInfo</>;
};

export default UserInfo;
